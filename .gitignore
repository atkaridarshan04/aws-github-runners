# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!terraform.tfvars.example
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Downloaded module Lambda zips — build artifacts, not source
terraform/lambda-packages/

# Scratch clone of the upstream repo for the Packer AMI builds
/terraform-aws-github-runner/
